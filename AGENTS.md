# AGENTS.md

Read `README.md` before changing `src/example.py`.
Keep changes scoped to the demonstration wound under review.
Run `python3 -m py_compile src/example.py` before handoff.
Do not suppress exceptions or hide failed validation.
